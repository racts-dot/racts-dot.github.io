# Your reminder, two ways. You do not have to understand any of it.

**Date:** 9 September 2026

---

## Way 1 — it is already automatic. Nothing for you to do.

A file called `CLAUDE.md` now sits inside the website folder. **Every AI that
opens that folder reads it first, without being asked.** It tells them the rule
and why it exists.

So in most cases you will never need the prompt below. It just happens.

---

## Way 2 — the prompt, for when you are somewhere else

Use this if you are on your phone, on the other computer, or in a chat that
cannot see your files. **Copy it, paste it, done.**

> Before you touch my Sales Tracker website: there must only ever be ONE copy of
> that folder, at `C:\Users\soyan\Desktop\Sales Tracker Website`. Never duplicate
> it, never put it in Downloads or Google Drive. To work from another computer,
> clone a fresh copy from GitHub instead of carrying the folder across. When you
> finish, always commit and push — and tell me plainly if the push fails.

---

## What it is actually protecting you from, in one line

**Two copies of the same folder drift apart, and then nobody can tell which one
holds the newest work.**

That happened this week. One copy held TripShare — a whole page, 1,210 lines,
existing in exactly one place and never sent to GitHub. You asked whether it was
safe to delete that folder. It was not. It was one click from gone.

One copy makes that impossible. That is the entire idea.

---

## The bit you do not need to understand

"Commit and push" means *save it to GitHub*. GitHub is the copy that lives away
from your computer, so if the laptop dies, the work does not.

You do not need to run those commands yourself. You just need to notice if an AI
says a push failed — that means the work is still only on your machine.

---

## Where things are now

- One folder, on the Desktop. The Downloads duplicate is gone.
- Both pages live: `racts-dot.github.io/app.html` and
  `racts-dot.github.io/tripshare/`
- Everything saved and sent up.
